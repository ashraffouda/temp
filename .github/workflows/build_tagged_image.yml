name: Build docker image

on:
  push:
    tags:
      - "*"  
jobs:
    build-docker:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - run: |
          echo "this is the current tag"
          echo "$github.ref"
